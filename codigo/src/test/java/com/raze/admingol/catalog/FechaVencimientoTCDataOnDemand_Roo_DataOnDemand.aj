// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.raze.admingol.catalog;

import com.raze.admingol.catalog.FechaVencimientoTC;
import com.raze.admingol.catalog.FechaVencimientoTCDataOnDemand;
import com.raze.admingol.domain.UsuarioDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect FechaVencimientoTCDataOnDemand_Roo_DataOnDemand {
    
    declare @type: FechaVencimientoTCDataOnDemand: @Component;
    
    private Random FechaVencimientoTCDataOnDemand.rnd = new SecureRandom();
    
    private List<FechaVencimientoTC> FechaVencimientoTCDataOnDemand.data;
    
    @Autowired
    UsuarioDataOnDemand FechaVencimientoTCDataOnDemand.usuarioDataOnDemand;
    
    public FechaVencimientoTC FechaVencimientoTCDataOnDemand.getNewTransientFechaVencimientoTC(int index) {
        FechaVencimientoTC obj = new FechaVencimientoTC();
        setActivo(obj, index);
        setAnio(obj, index);
        setDescripcion(obj, index);
        setFechaCreacion(obj, index);
        setFechaModificacion(obj, index);
        setMes(obj, index);
        setNombreFechaVencimientoTC(obj, index);
        return obj;
    }
    
    public void FechaVencimientoTCDataOnDemand.setActivo(FechaVencimientoTC obj, int index) {
        Boolean activo = Boolean.TRUE;
        obj.setActivo(activo);
    }
    
    public void FechaVencimientoTCDataOnDemand.setAnio(FechaVencimientoTC obj, int index) {
        int anio = index;
        obj.setAnio(anio);
    }
    
    public void FechaVencimientoTCDataOnDemand.setDescripcion(FechaVencimientoTC obj, int index) {
        String descripcion = "descripcion_" + index;
        obj.setDescripcion(descripcion);
    }
    
    public void FechaVencimientoTCDataOnDemand.setFechaCreacion(FechaVencimientoTC obj, int index) {
        Date fechaCreacion = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setFechaCreacion(fechaCreacion);
    }
    
    public void FechaVencimientoTCDataOnDemand.setFechaModificacion(FechaVencimientoTC obj, int index) {
        Date fechaModificacion = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setFechaModificacion(fechaModificacion);
    }
    
    public void FechaVencimientoTCDataOnDemand.setMes(FechaVencimientoTC obj, int index) {
        int mes = index;
        obj.setMes(mes);
    }
    
    public void FechaVencimientoTCDataOnDemand.setNombreFechaVencimientoTC(FechaVencimientoTC obj, int index) {
        String nombreFechaVencimientoTC = "nombreFechaVencimientoTC_" + index;
        obj.setNombreFechaVencimientoTC(nombreFechaVencimientoTC);
    }
    
    public FechaVencimientoTC FechaVencimientoTCDataOnDemand.getSpecificFechaVencimientoTC(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        FechaVencimientoTC obj = data.get(index);
        Long id = obj.getId();
        return FechaVencimientoTC.findFechaVencimientoTC(id);
    }
    
    public FechaVencimientoTC FechaVencimientoTCDataOnDemand.getRandomFechaVencimientoTC() {
        init();
        FechaVencimientoTC obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return FechaVencimientoTC.findFechaVencimientoTC(id);
    }
    
    public boolean FechaVencimientoTCDataOnDemand.modifyFechaVencimientoTC(FechaVencimientoTC obj) {
        return false;
    }
    
    public void FechaVencimientoTCDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = FechaVencimientoTC.findFechaVencimientoTCEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'FechaVencimientoTC' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<FechaVencimientoTC>();
        for (int i = 0; i < 10; i++) {
            FechaVencimientoTC obj = getNewTransientFechaVencimientoTC(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
