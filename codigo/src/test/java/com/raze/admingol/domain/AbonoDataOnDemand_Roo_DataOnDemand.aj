// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.raze.admingol.domain;

import com.raze.admingol.catalog.DescuentoDataOnDemand;
import com.raze.admingol.catalog.MetodoPago;
import com.raze.admingol.catalog.MetodoPagoDataOnDemand;
import com.raze.admingol.catalog.StatusCargoAbono;
import com.raze.admingol.catalog.StatusCargoAbonoDataOnDemand;
import com.raze.admingol.domain.Abono;
import com.raze.admingol.domain.AbonoDataOnDemand;
import com.raze.admingol.domain.Cargo;
import com.raze.admingol.domain.CargoDataOnDemand;
import com.raze.admingol.domain.UsuarioDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect AbonoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AbonoDataOnDemand: @Component;
    
    private Random AbonoDataOnDemand.rnd = new SecureRandom();
    
    private List<Abono> AbonoDataOnDemand.data;
    
    @Autowired
    CargoDataOnDemand AbonoDataOnDemand.cargoDataOnDemand;
    
    @Autowired
    DescuentoDataOnDemand AbonoDataOnDemand.descuentoDataOnDemand;
    
    @Autowired
    MetodoPagoDataOnDemand AbonoDataOnDemand.metodoPagoDataOnDemand;
    
    @Autowired
    StatusCargoAbonoDataOnDemand AbonoDataOnDemand.statusCargoAbonoDataOnDemand;
    
    @Autowired
    UsuarioDataOnDemand AbonoDataOnDemand.usuarioDataOnDemand;
    
    public Abono AbonoDataOnDemand.getNewTransientAbono(int index) {
        Abono obj = new Abono();
        setCargo(obj, index);
        setFechaCreacion(obj, index);
        setFechaModificacion(obj, index);
        setMetodoPago(obj, index);
        setObservaciones(obj, index);
        setStatus(obj, index);
        return obj;
    }
    
    public void AbonoDataOnDemand.setCargo(Abono obj, int index) {
        Cargo cargo = cargoDataOnDemand.getRandomCargo();
        obj.setCargo(cargo);
    }
    
    public void AbonoDataOnDemand.setFechaCreacion(Abono obj, int index) {
        Date fechaCreacion = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setFechaCreacion(fechaCreacion);
    }
    
    public void AbonoDataOnDemand.setFechaModificacion(Abono obj, int index) {
        Date fechaModificacion = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setFechaModificacion(fechaModificacion);
    }
    
    public void AbonoDataOnDemand.setMetodoPago(Abono obj, int index) {
        MetodoPago metodoPago = metodoPagoDataOnDemand.getRandomMetodoPago();
        obj.setMetodoPago(metodoPago);
    }
    
    public void AbonoDataOnDemand.setObservaciones(Abono obj, int index) {
        String observaciones = "observaciones_" + index;
        obj.setObservaciones(observaciones);
    }
    
    public void AbonoDataOnDemand.setStatus(Abono obj, int index) {
        StatusCargoAbono status = statusCargoAbonoDataOnDemand.getRandomStatusCargoAbono();
        obj.setStatus(status);
    }
    
    public Abono AbonoDataOnDemand.getSpecificAbono(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Abono obj = data.get(index);
        Long id = obj.getId();
        return Abono.findAbono(id);
    }
    
    public Abono AbonoDataOnDemand.getRandomAbono() {
        init();
        Abono obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Abono.findAbono(id);
    }
    
    public boolean AbonoDataOnDemand.modifyAbono(Abono obj) {
        return false;
    }
    
    public void AbonoDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Abono.findAbonoEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Abono' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Abono>();
        for (int i = 0; i < 10; i++) {
            Abono obj = getNewTransientAbono(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
